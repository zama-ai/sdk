# Copy to .env.local and adjust if your local servers use different ports.
NEXT_PUBLIC_ZAMA_RPC_URL=http://127.0.0.1:8545
NEXT_PUBLIC_INDEXER_URL=http://127.0.0.1:8787

# Only used by scripts/grant-delegation.mjs (one-time setup), never by the app itself.
# HOLDER_PK=0xTheCUsdcHolderKey
# DELEGATE_ADDRESS=0xConfidentialIndexersOperationalAddress
